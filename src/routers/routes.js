const loginUser = require('../controllers/login.controller');
const createUser = require('../controllers/createUser.controller');
const allUsers = require('../controllers/allUsers.controller');

module.exports = {
  loginUser, createUser, allUsers,
};